[project]
name = "builder-agent"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.39",
    "fastapi>=0.110",
    "httpx>=0.27",
    "pydantic>=2.6",
    "pytest>=8.0",
    "uvicorn>=0.27",
]
